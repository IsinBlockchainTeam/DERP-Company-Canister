{
    "name": "@derp/company-canister",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "scripts": {
        "pretest": "tsx test/pretest.ts",
        "test": "jest",
        "start-network": "dfx start --clean",
        "deploy": "set -o allexport; set +o allexport && dfx deploy",
        "build": "npm run generate && mkdir -p dist && cp -r ./src/declarations ./dist && npx tsc -p tsconfig-lib.json",
        "clean": "rm -rf dist && rm -rf src/declarations",
        "generate": "npm run clean && set -o allexport; set +o allexport && dfx build --all && dfx generate"
    },
    "dependencies": {
        "azle": "^0.24.1"
    },
    "devDependencies": {
        "@types/text-encoding": "^0.0.40",
        "jest": "^29.7.0",
        "ts-jest": "^29.1.4",
        "tsx": "^4.15.7",
        "typescript": "^5.2.2"
    }
}
